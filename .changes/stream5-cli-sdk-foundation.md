---
type: feature
surface: cli
---

Added local CLI and SDK foundations for idempotent init, local run evidence, inspectable
module capabilities, JSON output, targeted smoke tests, and a runnable local evidence
example. This is a local developer foundation only; provider runtime, tool gateway,
hosted workbench, hosted stores, docs generation, and release publishing remain open.
