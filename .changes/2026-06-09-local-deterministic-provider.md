# Local Deterministic Provider Foundation

Added a replaceable local deterministic provider package, SDK and CLI workflow wiring,
fail-closed external-provider routing, provider capability manifests, recoverable
fail-once fixtures, and tests proving the local provider path can execute through the
policy-gated tool gateway, checkpoints, artifacts, traces, memory/context, and evidence.
