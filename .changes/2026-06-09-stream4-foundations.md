# Stream 4 foundations

- Added artifact, observability, and memory package foundations with local replaceable
  defaults for artifact provenance, runtime evidence export, trace/audit references,
  permission-filtered memory recall, citation freshness, retention, redaction, and
  deterministic context replay.
- Extended contracts, generated references, and fixtures for `artifactRecord`,
  `traceEvent`, `memoryRecord`, and `contextPack`.
- Kept hosted stores, OpenTelemetry export, vector search, docs generation, CLI, and
  workbench surfaces explicitly out of scope for this pass.
