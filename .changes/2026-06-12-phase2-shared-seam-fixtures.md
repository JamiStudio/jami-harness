---
type: feature
surface: contracts
---

Extended the harness-side Phase 2 shared contract fixture matrix for run events, UI
payloads, artifact views, action refs, theme refs, suite refs, evidence packets,
memory records, context packs, and capability manifests. `pnpm contracts:validate`
now fails when a root-roadmap seam state is missing from machine-readable fixture
coverage.
