---
type: docs
surface: docs
---

Aligned the harness dispatch prompt, roadmap source-lock baseline, and durable development
workflow with the registry-root `jami-harness`/`studio-ui` boundary and docs-only versus
implementation verification gates. Verification: read back changed Markdown, `pnpm docs:check`,
and `git diff --check`.
