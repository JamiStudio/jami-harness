# Contract Spine

- Added the initial `@jami-studio/harness-contracts` package with JSON Schema anchors,
  compatibility fixtures, and a validation command for the harness-owned UI/action/artifact
  seam.
- Wired `pnpm verify` to run contract validation in addition to docs checks.
