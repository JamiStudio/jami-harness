# Release Capability Manifest

Added a generated release/hosted capability manifest with check-mode drift protection.
The manifest records official-source prerequisites for npm provenance, GitHub
attestations, and Mintlify validation while keeping hosted docs, hosted providers,
hosted stores, hosted workbench, package publishing, and attestation surfaces fail-closed
until executable evidence exists.
